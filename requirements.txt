# Core RAG framework
# Pinned below 0.4/1.0: langchain-community>=0.4 dropped the legacy
# `langchain_community.chat_models.vertexai` shim that ragas (see Evaluation
# section) imports eagerly at package import time regardless of provider,
# so newer langchain-community breaks `import ragas` outright.
langchain>=0.3.27,<0.4.0
langchain-core>=0.3.66,<1.0.0
langchain-community>=0.3.27,<0.4.0
langchain-openai>=0.2.14,<0.3.0

# LLM provider (reserved for the generation/chat step, not currently used by ingestion)
openai>=1.30.0
tiktoken>=0.7.0

# Embeddings provider (used by ingestion/build_index.py)
google-genai>=1.0.0

# Vector database (MongoDB Atlas Vector Search)
pymongo>=4.9.0

# Document loading (PDF/text ingestion)
pypdf>=4.2.0
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Source retrieval (data/raw ingestion scripts)
requests>=2.31.0

# App / UI
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
streamlit>=1.35.0

# Config management
python-dotenv>=1.0.0

# Evaluation (eval/run_ragas.py): RAGAS metrics + golden-set loading.
# ragas pinned <0.3.0 for compatibility with the langchain family pinned
# above (see the note there) — newer ragas releases carry the same
# eager-import break against langchain-community>=0.4.
ragas>=0.2.15,<0.3.0
datasets>=2.19.0
pyyaml>=6.0
